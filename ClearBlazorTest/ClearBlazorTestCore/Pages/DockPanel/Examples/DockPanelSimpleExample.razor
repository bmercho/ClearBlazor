<DockPanel BackgroundColour="@Color.BackgroundGrey">
    <Border Dock="Dock.Left" Width=200 BackgroundColour="@Color.Custom("lightblue")">
        <TextBlock HorizontalAlignment="Alignment.Center" VerticalAlignment="Alignment.Center">Left1</TextBlock>
    </Border>
    <Border Dock="Dock.Top" Height=200 BackgroundColour="@Color.Custom("red")">
        <TextBlock HorizontalAlignment="Alignment.Center" VerticalAlignment="Alignment.Center">Top1</TextBlock>
    </Border>
    <Border Dock="Dock.Bottom" Height=200 BackgroundColour="@Color.Custom("yellow")">
        <TextBlock HorizontalAlignment="Alignment.Center" VerticalAlignment="Alignment.Center">Bottom</TextBlock>
    </Border>
    <Border Dock="Dock.Left" Width=100 BackgroundColour="@Color.Custom("green")">
        <TextBlock HorizontalAlignment="Alignment.Center" VerticalAlignment="Alignment.Center">Left</TextBlock>
    </Border>
    <Border Dock="Dock.Right" Width=100 BackgroundColour="@Color.Custom("orange")">
        <TextBlock HorizontalAlignment="Alignment.Center" VerticalAlignment="Alignment.Center">Right</TextBlock>
    </Border>
    <Border BackgroundColour="@Color.Custom("pink")">
        <TextBlock HorizontalAlignment="Alignment.Center" VerticalAlignment="Alignment.Center">Centre</TextBlock>
    </Border>
</DockPanel>
