<Border Height="200" Width="200" CornerRadius="0" BackgroundColour=@Color.Custom("lightblue")>
    <Border BorderThickness="0" Width="75" Height="75" BackgroundColour="@Color.Custom("blue")"
            HorizontalAlignment="Alignment.Start" VerticalAlignment=Alignment.Start>
        <TextBlock HorizontalAlignment="Alignment.Center" VerticalAlignment=Alignment.Center>Left Top</TextBlock>
    </Border>
</Border>
<Border Height="200" Width="200" CornerRadius="0" BackgroundColour=@Color.Custom("lightblue")>
    <Border BorderThickness="0" Width="75" Height="75" BackgroundColour="@Color.Custom("green")"
            HorizontalAlignment="Alignment.Start" VerticalAlignment="Alignment.End">
        <TextBlock HorizontalAlignment="Alignment.Center" VerticalAlignment=Alignment.Center>Left Bottom</TextBlock>
    </Border>
</Border>
<Border Height="200" Width="200" CornerRadius="0" BackgroundColour=@Color.Custom("lightblue")>
    <Border BorderThickness="0" Width="75" Height="75" BackgroundColour="@Color.Custom("red")"
            HorizontalAlignment="Alignment.Start" VerticalAlignment="Alignment.Center">
        <TextBlock HorizontalAlignment="Alignment.Center" VerticalAlignment=Alignment.Center>Left Center</TextBlock>
    </Border>
</Border>
<Border Height="200" Width="200" CornerRadius="0" BackgroundColour=@Color.Custom("lightblue")>
    <Border BorderThickness="0" Width="75" Height="75" BackgroundColour="@Color.Custom("yellow")"
            HorizontalAlignment="Alignment.End" VerticalAlignment="Alignment.Start">
        <TextBlock HorizontalAlignment="Alignment.Center" VerticalAlignment=Alignment.Center>Right Top</TextBlock>
    </Border>
</Border>
<Border Height="200" Width="200" CornerRadius="0" BackgroundColour=@Color.Custom("lightblue")>
    <Border BorderThickness="0" Width="75" Height="75" BackgroundColour="@Color.Custom("purple")"
            HorizontalAlignment="Alignment.End" VerticalAlignment="Alignment.End">
        <TextBlock HorizontalAlignment="Alignment.Center" VerticalAlignment=Alignment.Center>Right Bottom</TextBlock>
    </Border>
</Border>
<Border Height="200" Width="200" CornerRadius="0" BackgroundColour=@Color.BackgroundGrey>
    <Border BorderThickness="0" Width="75" Height="75" BackgroundColour="@Color.Custom("pink")"
            HorizontalAlignment="Alignment.End" VerticalAlignment="Alignment.Center">
        <TextBlock HorizontalAlignment="Alignment.Center" VerticalAlignment=Alignment.Center>Right Centre</TextBlock>
    </Border>
</Border>
<Border Height="200" Width="200" CornerRadius="0" BackgroundColour=@Color.Custom("lightblue")>
    <Border BorderThickness="0" Width="75" Height="75" BackgroundColour="@Color.Custom("violet")"
            HorizontalAlignment="Alignment.Center" VerticalAlignment="Alignment.Start">
        <TextBlock HorizontalAlignment="Alignment.Center" VerticalAlignment=Alignment.Center>Centre Top</TextBlock>
    </Border>
</Border>
<Border Height="200" Width="200" CornerRadius="0" BackgroundColour=@Color.Custom("lightblue")>
    <Border BorderThickness="0" Width="75" Height="75" BackgroundColour="@Color.Custom("orange")"
            HorizontalAlignment="Alignment.Center" VerticalAlignment="Alignment.End">
        <TextBlock HorizontalAlignment="Alignment.Center" VerticalAlignment=Alignment.Center>Centre Bottom</TextBlock>
    </Border>
</Border>
<Border Height="200" Width="200" CornerRadius="0" BackgroundColour=@Color.Custom("lightblue")>
    <Border BorderThickness="0" Width="75" Height="75" BackgroundColour="@Color.Custom("aqua")"
            HorizontalAlignment="Alignment.Center" VerticalAlignment="Alignment.Center">
        <TextBlock HorizontalAlignment="Alignment.Center" VerticalAlignment=Alignment.Center>Centre</TextBlock>
    </Border>
</Border>

