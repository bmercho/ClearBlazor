@namespace ClearBlazorTest
<Grid Height="200" Width="200" CornerRadius="0" BackgroundColour=@Color.Custom("lightblue")>
    <Grid BorderThickness="0" Width="75" Height="75" BackgroundColour="@Color.Custom("blue")"
            HorizontalAlignment="Alignment.Start" VerticalAlignment=Alignment.Start>
        <TextBlock TextAlignment=Alignment.Center HorizontalAlignment="Alignment.Center" 
                   VerticalAlignment=Alignment.Center>Left Top</TextBlock>
    </Grid>
</Grid>
<Grid Height="200" Width="200" CornerRadius="0" BackgroundColour=@Color.Custom("lightblue")>
    <Grid BorderThickness="0" Width="75" Height="75" BackgroundColour="@Color.Custom("green")"
            HorizontalAlignment="Alignment.Start" VerticalAlignment="Alignment.End">
        <TextBlock TextAlignment=Alignment.Center HorizontalAlignment="Alignment.Center" 
                   VerticalAlignment=Alignment.Center>Left Bottom</TextBlock>
    </Grid>
</Grid>
<Grid Height="200" Width="200" CornerRadius="0" BackgroundColour=@Color.Custom("lightblue")>
    <Grid BorderThickness="0" Width="75" Height="75" BackgroundColour="@Color.Custom("red")"
            HorizontalAlignment="Alignment.Start" VerticalAlignment="Alignment.Center">
        <TextBlock TextAlignment=Alignment.Center HorizontalAlignment="Alignment.Center" 
                   VerticalAlignment=Alignment.Center>Left Center</TextBlock>
    </Grid>
</Grid>
<Grid Height="200" Width="200" CornerRadius="0" BackgroundColour=@Color.Custom("lightblue")>
    <Grid BorderThickness="0" Width="75" Height="75" BackgroundColour="@Color.Custom("yellow")"
            HorizontalAlignment="Alignment.End" VerticalAlignment="Alignment.Start">
        <TextBlock TextAlignment=Alignment.Center HorizontalAlignment="Alignment.Center" 
                   VerticalAlignment=Alignment.Center>Right Top</TextBlock>
    </Grid>
</Grid>
<Grid Height="200" Width="200" CornerRadius="0" BackgroundColour=@Color.Custom("lightblue")>
    <Grid BorderThickness="0" Width="75" Height="75" BackgroundColour="@Color.Custom("purple")"
            HorizontalAlignment="Alignment.End" VerticalAlignment="Alignment.End">
        <TextBlock TextAlignment=Alignment.Center HorizontalAlignment="Alignment.Center" 
                   VerticalAlignment=Alignment.Center>Right Bottom</TextBlock>
    </Grid>
</Grid>
<Grid Height="200" Width="200" CornerRadius="0" BackgroundColour=@Color.BackgroundGrey>
    <Grid BorderThickness="0" Width="75" Height="75" BackgroundColour="@Color.Custom("pink")"
            HorizontalAlignment="Alignment.End" VerticalAlignment="Alignment.Center">
        <TextBlock TextAlignment=Alignment.Center HorizontalAlignment="Alignment.Center" 
                   VerticalAlignment=Alignment.Center>Right Centre</TextBlock>
    </Grid>
</Grid>
<Grid Height="200" Width="200" CornerRadius="0" BackgroundColour=@Color.Custom("lightblue")>
    <Grid BorderThickness="0" Width="75" Height="75" BackgroundColour="@Color.Custom("violet")"
            HorizontalAlignment="Alignment.Center" VerticalAlignment="Alignment.Start">
        <TextBlock TextAlignment=Alignment.Center HorizontalAlignment="Alignment.Center" 
                   VerticalAlignment=Alignment.Center>Centre Top</TextBlock>
    </Grid>
</Grid>
<Grid Height="200" Width="200" CornerRadius="0" BackgroundColour=@Color.Custom("lightblue")>
    <Grid BorderThickness="0" Width="75" Height="75" BackgroundColour="@Color.Custom("orange")"
            HorizontalAlignment="Alignment.Center" VerticalAlignment="Alignment.End">
        <TextBlock TextAlignment=Alignment.Center HorizontalAlignment="Alignment.Center" 
                   VerticalAlignment=Alignment.Center>Centre Bottom</TextBlock>
    </Grid>
</Grid>
<Grid Height="200" Width="200" CornerRadius="0" BackgroundColour=@Color.Custom("lightblue")>
    <Grid BorderThickness="0" Width="75" Height="75" BackgroundColour="@Color.Custom("aqua")"
            HorizontalAlignment="Alignment.Center" VerticalAlignment="Alignment.Center">
        <TextBlock TextAlignment=Alignment.Center HorizontalAlignment="Alignment.Center" 
                   VerticalAlignment=Alignment.Center>Centre</TextBlock>
    </Grid>
</Grid>

