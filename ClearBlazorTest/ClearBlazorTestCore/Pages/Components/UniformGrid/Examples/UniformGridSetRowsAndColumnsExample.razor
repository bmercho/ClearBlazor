@namespace ClearBlazorTest
<UniformGrid NumRows=2 NumColumns=2>
    <Border BackgroundColour="@Color.Custom("lightblue")">
        <TextBlock HorizontalAlignment="Alignment.Center" VerticalAlignment="Alignment.Center">1</TextBlock>
    </Border>
    <Border BackgroundColour="@Color.Custom("red")">
        <TextBlock HorizontalAlignment="Alignment.Center" VerticalAlignment="Alignment.Center">2</TextBlock>
    </Border>
    <Border BackgroundColour="@Color.Custom("yellow")">
        <TextBlock HorizontalAlignment="Alignment.Center" VerticalAlignment="Alignment.Center">3</TextBlock>
    </Border>
    <Border BackgroundColour="@Color.Custom("green")">
        <TextBlock HorizontalAlignment="Alignment.Center" VerticalAlignment="Alignment.Center">4</TextBlock>
    </Border>
    <Border BackgroundColour="@Color.Custom("orange")">
        <TextBlock HorizontalAlignment="Alignment.Center" VerticalAlignment="Alignment.Center">5</TextBlock>
    </Border>
    <Border BackgroundColour="@Color.Custom("pink")">
        <TextBlock HorizontalAlignment="Alignment.Center" VerticalAlignment="Alignment.Center">6</TextBlock>
    </Border>
    <Border BackgroundColour="@Color.Custom("aqua")">
        <TextBlock HorizontalAlignment="Alignment.Center" VerticalAlignment="Alignment.Center">7</TextBlock>
    </Border>
</UniformGrid>

