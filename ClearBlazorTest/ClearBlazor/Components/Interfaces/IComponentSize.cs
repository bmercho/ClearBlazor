namespace ClearBlazor
{
    /// <summary>
    /// This is an empty interface. If a component implements it then it indicates that ClearComponentBase 
    /// should get and maintain the size of the component. This is only required if the component needs its size.
    /// </summary>
    public interface IComponentSize
    {
    }
}
