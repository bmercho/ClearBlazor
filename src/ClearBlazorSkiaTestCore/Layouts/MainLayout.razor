@using ClearBlazorSkia
@using Microsoft.AspNetCore.Components.Web.Virtualization
@using System.ComponentModel
@using System.ComponentModel.DataAnnotations
@using System.Timers
@using ClearBlazor
@namespace ClearBlazorTest
@inherits LayoutComponentBase
@inject NavigationManager NavManager

<RootComponent BackgroundColor=@Color.Custom("red") Width=500 Height=400>
    @* <Grid Name="Grid1" Padding=0 BorderThickness="10" HorizontalAlignment=Alignment.Stretch Width=200 BackgroundColor=@Color.Custom("yellow")> *@
@*         <StackPanel>
            <Grid Height=200 BackgroundColor ="@Color.Custom("lightblue")" CornerRadius="0">
            </Grid>
            <Grid Height=200 BackgroundColor="@Color.Custom("red")" CornerRadius="0">
            </Grid>
            <Grid Height=200 BackgroundColor="@Color.Custom("yellow")" CornerRadius="0">
            </Grid>
            <Grid Height=200 BackgroundColor="@Color.Custom("green")" CornerRadius="0">
            </Grid>
            <Grid Height=200 BackgroundColor="@Color.Custom("orange")" CornerRadius="0">
            </Grid>
            <Grid Height=200 BackgroundColor="@Color.Custom("pink")" CornerRadius="0">
            </Grid>
        </StackPanel>
 *@  
@* </Grid> *@
</RootComponent>


@code {
}